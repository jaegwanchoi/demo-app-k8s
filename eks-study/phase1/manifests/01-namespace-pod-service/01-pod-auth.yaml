apiVersion: v1
kind: Pod
metadata:
  name: auth
  namespace: app-dev
  labels:
    app: auth
    tier: backend
spec:
  containers:
    - name: auth
      image: hashicorp/http-echo:latest
      args:
        - "-text=auth-ok"
        - "-listen=:8080"
      ports:
        - containerPort: 8080

