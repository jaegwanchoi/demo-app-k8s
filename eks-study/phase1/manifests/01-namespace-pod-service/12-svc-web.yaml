apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: app-dev
spec:
  type: NodePort       # 외부 접근용
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080  # 노드의 30080 포트로 외부 노출

